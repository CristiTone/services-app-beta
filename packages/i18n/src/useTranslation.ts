/**
 * useTranslation wrapper - works on both web and mobile.
 * Re-export from 'react-i18next' in apps; this package only provides init and types.
 * Apps should install react-i18next and call initI18n() at bootstrap.
 */
export type { TFunction } from 'i18next';
