# Hoist Prisma packages to root node_modules so the generated client
# (.prisma/client/) is accessible from @prisma/client in the pnpm virtual store
public-hoist-pattern[]=*prisma*
public-hoist-pattern[]=.prisma
