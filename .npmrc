# Use flat node_modules so Prisma's generated client (.prisma/client/) is
# adjacent to @prisma/client — pnpm symlinks cause Node to resolve relative
# requires from the real pnpm store path where the generated files don't exist.
shamefully-hoist=true
